services:
  scontrino-webapp:
    build: .
    ports:
      - "8001:8000"
    environment:
      - OPENAI_API_KEY
      - DB_PATH=/data/buchhaltung.db
    volumes:
      - buchhaltung_data:/data
    restart: unless-stopped

volumes:
  buchhaltung_data:
